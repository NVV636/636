#pragma once
#include <stdlib.h>
#include <stdio.h>
#include "f.c"
int Comp(char* str1, const char* str2);
char* StrSubstr(char* str1, char* str2);
int StrLen(char* str);
void PrintString(char* str);