#include "func.h"
int main()
{
	Matrices* Matrix1 = CreationMatrix(3, 3);
	Matrices* Matrix2 = CreationMatrix(3, 3);
	void F(Matrix1);
	void F(Matrix2);
	PrintM(Matrix1);
	PrintM(Matrix2);

}